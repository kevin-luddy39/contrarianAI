"""LlamaIndex adapter — hooks into the QueryEngine / ChatEngine pipeline.

TODO: target the QueryPipeline + ChatEngine.chat hooks so retrieval
output and tool-call output can be streamed into the conductor sensors
without forking the LlamaIndex core.
"""

raise NotImplementedError("llamaindex.py not yet implemented")
